<!DOCTYPE html>
<html lang="fr">
	<head>
		<meta charset="UTF-8">
		<meta http-equiv="X-UA-Compatible" content="IE=edge">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<title>Avatar</title>
		<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
		<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-KyZXEAg3QhqLMpG8r+8fhAXLRk2vvoC2f3B09zVXn8CA5QIVfZOJ3BCsw2P0p/We" crossorigin="anonymous">
		<link rel="stylesheet" href="/css/style.css">
	</head>
	<body>
		<div class="container">
			<header>
				<h1 class="text-center my-5">Créez votre avatar !</h1>
			</header>
			<main>
				<div class="row d-flex justify-content-center">
					<div class="col-4 d-flex flex-column align-items-center">
						{{ svg|raw }}

						<nav class="d-flex mt-3">
							<form action="{{ path('avatar_save') }}" method="post" class="px-3">
								<input type="hidden" name="svg" value="{{ svg }}">
								<button type="submit" class="btn btn-primary mr-3">
									<i class="far fa-save"></i>
									Enregistrer</button>
							</form>

							<form action="{{ path('avatar_download') }}" method="post">
								<input type="hidden" name="svg" value="{{ svg }}">
								<button type="submit" class="btn btn-primary">
									<i class="fas fa-file-download"></i>
									Télécharger</button>
							</form>
						</nav>
					</div>
					<div class="col-4">
						{{ form_start(formView) }}

						{{ form_row(formView.colorAmount) }}
						{{ form_row(formView.avatarSize) }}
						<button class="btn btn-primary mt-3" type="submit">
							<i class="fas fa-redo"></i>
							Générer un avatar</button>

						{{ form_end(formView) }}

						{# <form action="{{ path('home') }}" method="post">
																																																																																																	<div class="mb-3">
																																																																																																		<label class="form-label" for="color-amount">
																																																																																																			<i class="fas fa-palette"></i>
																																																																																																			Nombre de couleurs</label>
																																																																																																		<input class="form-control w-50" type="number" required min="2" max="5" id="color-amount" name="color-amount" value="{{ colorAmount }}">
																																																																																																	</div>
																																																																																																	<div class="mb-3">
																																																																																																		<label class="form-label" for="avatar-size">
																																																																																																			<i class="fas fa-th"></i>
																																																																																																			Taille de l'avatar</label>
																																																																																																		<input class="form-control w-50" type="number" required min="3" max="10" id="avatar-size" name="avatar-size" value="{{ avatarSize }}">
																																																																																																	</div>
																																																																																																	<button class="btn btn-primary" type="submit">
																																																																																																		<i class="fas fa-redo"></i>
																																																																																																		Générer un avatar</button>
																																																																																																</form> #}
					</div>
				</div>
				<div class="mt-3 row d-flex justify-content-center">
					<div class="col-8">
						{% if avatar %}
							<h2>Dernier avatar enregistré</h2>
							<img id="last-saved-avatar" src="/avatars/{{ avatar }}" alt="">
						{% endif %}
					</div>
				</div>
			</main>
		</div>
	</body>
</html></body></html>
